# OS/editor noise
.DS_Store
Thumbs.db

# Build/dev artifacts
__pycache__/
*.pyc
.venv/
dist/
.pytest_cache/

# built bench binaries (spikes/parity/mux-tax)
spikes/parity/mux-tax/qcb-stamp
spikes/parity/mux-tax/muxsend.o

# Rust build dir (was committed by accident on main, 2026-09-22)
agent/target/

# The capture helper crate has its own target dir (same reason as agent/target)
agent/capture-win/target/
