# OS/editor noise
.DS_Store
Thumbs.db

# Build/dev artifacts
__pycache__/
*.pyc
.venv/
dist/
.pytest_cache/


# Rust build dir (was committed by accident on main, 2026-09-22)
agent/target/

# The capture helper crate has its own target dir (same reason as agent/target)
agent/capture-win/target/

# local notes, spikes and anything else that stays on this machine
private/
