[package]
name = "qcb-icons"
version = "0.1.0"
edition = "2024"
publish = false
description = "Renders the QCBridge tray, window and app icons from the SVG sources into agent/assets/icons"

[workspace]

[dependencies]
resvg = "0.45"
tiny-skia = "0.11"
png = "0.17"
